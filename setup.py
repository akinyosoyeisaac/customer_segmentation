from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description=' To build a machine learning model that use customer segmentation to define marketing strategy',
    author='Isaac',
    license='MIT',
)
